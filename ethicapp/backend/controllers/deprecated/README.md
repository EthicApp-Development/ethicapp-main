# Deprecated route controllers

This directory preserves legacy route controllers that are no longer mounted by
the active EthicApp Express application.

They are kept as implementation notes for possible future rework, especially
where they contain useful SQL queries or flow logic. Do not add new routes here;
prefer active controllers under `ethicapp/backend/controllers/`.
