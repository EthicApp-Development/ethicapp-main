# EthicApp Deployment

TODO.

The production deployment strategy is being redesigned. The new deployment flow will be maintained in a parallel repository named `ethicapp-deploy`.

That repository will provide the deployment template structure and operational guidance for deploying EthicApp from images published in Google Container Registry (GCR).
