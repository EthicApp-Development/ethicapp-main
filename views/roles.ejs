<!DOCTYPE html>
<html lang="es" ng-app="Role">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>
    <title>DocCollab</title>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.1/jquery.min.js"></script>
    <script src="libs/angular.min.js"></script>
    <script src="libs/angular-sanitize.min.js"></script>
    <script src="libs/ui-bootstrap-tpls-1.1.2.min.js"></script>
    <script src="libs/angular-ui-tree.min.js"></script>
    <script src="libs/angular-timer.min.js"></script>
    <script src="libs/socket.min.js"></script>
    <script src="libs/angular-ui-notification.min.js"></script>
    <script src="libs/angular-glue.min.js"></script>
    <script src="libs/intro.min.js"></script>
    <script src="libs/angular-intro.min.js"></script>
    <script src="js/role-ctrl.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <link rel="stylesheet" href="css/bootstrap.min.css"/>
    <link rel="stylesheet" href="css/font-awesome.min.css">
    <link rel="stylesheet" href="css/minimal.css">
    <link rel="stylesheet" href="css/angular-ui-tree.min.css">
    <link rel="stylesheet" href="css/angular-ui-notification.min.css">
    <link rel="stylesheet" href="css/introjs.css">
    <link rel="stylesheet" href="css/main.css"/>
</head>
<body>

<div class="editor-container" ng-controller="RoleController">

    <div class="top-bar-mini cflex">
        <div class="cf1">
            <a href="seslist" class="simple-margin-left" id="seslistbtn">
                <img src="img/logo.png" class="logo-bar">
            </a>
        </div>
        <div class="cf3 grow text-center simple-padding-top hscroll" ng-show="iteration > 0">
<!--            <span ng-repeat="st in stages">-->
<!--                <span class="label label-primary bg-black-alpha" ng-class="{'bg-yellow': st.id==currentStageId}">{{ 'stage' | lang}} {{ st.number }}</span>-->
<!--                &lt;!&ndash; <span ng-hide="$last">→</span> &ndash;&gt;-->
<!--            </span>-->
<!--            <span class="label label-primary bg-black-alpha bg-yellow" ng-if="finished">{{ 'finished' | lang}}</span>-->
<!--            <i class="fa fa-angle-right" ng-if="!finished"></i>-->
            <!-- <span class="pull-right margin-right-24" ng-show="sesSTime != null">
                <timer start-time="sesSTime">{{hours}}h {{minutes}}m {{seconds}}s</timer>
            </span> -->
        </div>
        <div class="cf1 text-right margin-right-8 simple-padding-top">
            <a href="#" class="text-white margin-right-8" ng-click="startTour()">
                <i class="fa fa-info-circle fa-2x"></i>
            </a>
            <a href="logout" class="text-white">
                <i class="fa fa-sign-out fa-2x"></i>
            </a>
        </div>
    </div>

    <!--<div class="top-bar-mini">
        <span class="boxed" ng-bind="teamstr"></span>
    </div>-->

    <!-- <div class="top-bar-mini">
        <div class="text-center">
            <span ng-repeat="st in sesStatusses">
                <span class="label label-primary bg-black-alpha" ng-class="{'bg-yellow': st.i == iteration}" ng-bind="st.name"></span>
                <span ng-hide="$last">→</span>
            </span>
            <span class="pull-right margin-right-24" ng-show="sesSTime != null">
                <timer start-time="sesSTime">{{hours}}h {{minutes}}m {{seconds}}s</timer>
            </span>
        </div>
    </div> -->

    <!--<div class="top-bar-mini simple-padding-bottom">
        <strong>Factor detonante: </strong>
        <span style="color: yellow; font-size: 105%;" ng-bind="sesDescr"></span>
    </div>-->

    <div class="top-bar top-bar-tabs">
        <div class="qs-tabs" ng-show="iteration > 0 || myRole == 'P'">
            <div class="tab tab-text" ng-repeat="doc in documents" ng-class="{active: selectedDocument == $index && showDoc}"
                 ng-click="selectDocument($index)" ng-attr-id="{{ 'tabd' + $index }}">
                <i class="fa fa-book"></i> {{doc.title | limitTo:10}}
            </div>
            <div class="tab tab-text" ng-if="isJigsaw" ng-click="selectRoleDescr()" ng-class="{active: showRole}">
                <i class="fa fa-user"></i> {{ 'role' | lang }}
            </div>
            <div class="tab tab-text" ng-repeat="st in stages" ng-class="{active: selectedStage == $index && (!showDoc || documents.length==0) , 'green': st.id != currentStageId, 'yellow': st.id == currentStageId}"
                 ng-click="selectStage($index)" ng-attr-id="{{ 'tabq' + $index }}">
                <i class="fa fa-question-circle"></i> {{ 'stage' | lang }} {{st.number}} <span class="badge bg-red" ng-show="st.c != null && st.c > st.cr">{{st.c - st.cr}}</span>
            </div>
            <!--<div class="btab"></div>
            <div class="rtab">
                <button class="btn btn-sm margin-right-8 btn-default" ng-if="iteration > 0 && iteration < 4" ng-click="finishState()" ng-class="{'btn-success': finished, 'border-white': finished}">
                    <i class="fa fa-check"></i></button>
            </div>-->
        </div>
    </div>

    <!--<div class="top-bar top-bar-student">
        <div class="col-sm-6 full-height">
            <span class="simple-margin-left" style="vertical-align: top;">Documentos: </span>
            <span ng-repeat="doc in documents" class="side-top-btn" ng-class="{'active': $index==selectedDocument, 'border-orange': hgDocs[$index]}" ng-click="selectDocument($index)">
                {{doc.title | limitTo:16}}
            </span>
            <strong class="pull-right margin-top-8" ng-bind="iterationNames[iteration]"></strong>--
        </div>
        <div class="col-sm-6">
            <a onclick="window.history.back()" class="btn btn-sm btn-default pull-right">Atrás</a>
            <button disabled class="btn btn-sm btn-default pull-right" ng-show="iteration == 3 && !followLeader"><i class="fa fa-star"></i> Representante </button>
            <button class="btn btn-sm btn-default pull-right" ng-show="iteration == 3 && followLeader" ng-click="takeControl()">Tomar Control</button>
            <a class="btn btn-sm btn-default pull-right" ng-show="iteration > 6" href="rubrica">Ir a evaluación</a>
            <button class="btn btn-sm btn-default pull-right" ng-if="iteration > 0" ng-click="finishState()" ng-class="{'btn-success': hasFinished || finished}" ng-hide="myRole == 'P' && iteration > 0">
                <i class="fa fa-check"></i> Terminé</button>
            <span class="pull-right boxed" ng-bind="teamstr"></span>
        </div>
    </div>-->

    <div class="scrollable-container blue">
    <div class="mid-container qs-content simple-pad">
    <div class="boxed" ng-show="iteration > 0 && showDoc" ng-if="documents.length>0">
        <div>
            <h3>{{documents[selectedDocument].title}}</h3>
            <div ng-if="documents[selectedDocument].path != null">
                <iframe ng-src="{{getDocURL()}}" class="pdf-embed">
                </iframe>
            </div>
        </div>
    </div>
    <div ng-show="showRole">
        <h4>{{ 'roleAssigned' | lang }}: {{ jigsawMap[myJigsaw].name }}</h4>
        <div class="bordered role-box">
            {{ jigsawMap[myJigsaw].description }}
        </div>
        <strong>{{ 'roles' | lang }}: </strong>
        <span ng-repeat="j in jroles">{{ j.name }} - </span>
    </div>
    <div ng-show="iteration > 0 && (!showDoc || documents.length==0) && !showRole">
        <!--<h4>DF{{dfs[selectedDF].orden}}</h4>-->
        <div class="differential margin-top-n12">
            <h5>
                <span>{{stages[selectedStage].question}}</span>
            </h5>
            <div class="row" ng-if="stages[selectedStage].id == currentStageId">
                <div class="col-sm-10 col-sm-offset-1 col-xs-12">
                    <div ui-tree data-max-depth="1">
                        <div class="list-group" ui-tree-nodes="" ng-model="actors">
<!--                            <span class="text-gray center-block text-center" ng-show="selections.length > 0">Más importante</span>-->
                            <div class="list-group-item drag-item" ng-repeat="actor in actors" ui-tree-node>
                                <div class="row">
                                    <div class="col-xs-2 col-sm-1 text-center h30" ui-tree-handle ng-if="selectedActor == null">
                                        <span class="font-large">:::</span>
                                    </div>
                                    <div class="col-xs-2 col-sm-1" ng-if="selectedActor != null">

                                    </div>
                                    <div class="col-xs-8 col-sm-10 mh30">
                                       <strong>{{ $index + 1 }}. {{ actor.name }}</strong>
                                    </div>
                                    <div class="col-xs-2 col-sm-1 text-right">
<!--                                        <i class="fa fa-check margin-right-8" ng-if="actor.sent && ((!justifyPosition && actor.justified) || (justifyPosition && posToJustify[$index]))"></i>-->
                                        <button class="btn btn-default btn-sm" ng-click="selectActor($index)" ng-class="{'bg-green': actor.sent && ((!justifyPosition && actor.justified) || (justifyPosition && posToJustify[$index]))}"
                                                ng-if="((!justifyPosition && actor.justified) || (justifyPosition && posToJustify[$index])) && selectedActor == null">
                                            <i class="fa fa-edit"></i>
                                        </button>
                                    </div>
                                    <div class="col-xs-12" ng-if="selectedActor == $index && stages[selectedStage].id == currentStageId">
                                        <div class="input-group">
                                            <span class="input-group-addon word-count-r"
                                                  ng-class="{'bg-red': wordCount(actors[selectedActor].comment) < actors[selectedActor].word_count }">
                                                <span>{{ wordCount(actors[selectedActor].comment) }}</span>
                                                <br ng-if="actors[selectedActor].word_count">
                                                <span style="font-size: 80%" ng-if="actors[selectedActor].word_count">de</span>
                                                <br ng-if="actors[selectedActor].word_count">
                                                <span ng-if="actors[selectedActor].word_count">{{ actors[selectedActor].word_count }}</span>
                                            </span>
                                            <textarea type="text" class="form-control" placeholder="{{getPlaceholder()}}" ng-model="actors[selectedActor].comment"
                                                      ng-change="actors[selectedActor].dirty = true" ng-disabled="selectedActor == null">
                                            </textarea>
                                            <span class="input-group-addon hoverable" ng-disabled="finished || !actors[selectedActor].dirty || selectedActor == null"
                                                  ng-click="sendActorSel()">
                                            <i class="fa fa-check" ng-show="!actors[selectedActor].dirty"></i>
                                            <span ng-show="!actors[selectedActor].dirty">Enviado</span>
                                            <i class="fa fa-send" ng-show="actors[selectedActor].dirty"></i>
                                            <span ng-show="actors[selectedActor].dirty">Por Enviar</span>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
<!--                            <span class="text-gray center-block text-center" ng-show="selections.length > 0">Menos importante</span>-->
                        </div>
                    </div>
                </div>
            </div>
            <div class="row" ng-if="stages[selectedStage].id != currentStageId">
                <div class="col-sm-10 col-sm-offset-1 col-xs-12">
                    <div>
                        <div class="list-group" ng-model="actors">
                            <!--                            <span class="text-gray center-block text-center" ng-show="selections.length > 0">Más importante</span>-->
                            <div class="list-group-item drag-item" ng-repeat="actor in actorsPrev" ng-class="{'deep-blue': selectedActorPrev == $index}">
                                <div class="row">
                                    <div class="col-xs-2 col-sm-1 h30 text-center">
                                        {{$index + 1}}.
                                    </div>
                                    <div class="col-xs-6 col-sm-7 mh30">
                                        <strong>{{ actor.name }}</strong>
                                    </div>
                                    <div class="col-xs-4 text-right">
                                        <button class="btn btn-default btn-sm" ng-click="actor.expanded = !actor.expanded" ng-if="actor.justified && actor.comment != ''">
                                            <i class="fa fa-eye"></i>
                                        </button>
                                    </div>
                                </div>
                                <div class="row" ng-show="!actor.expanded">
                                    <div class="col-xs-12">
                                        {{ actor.comment }}
                                    </div>
                                </div>
                            </div>
                            <!--                            <span class="text-gray center-block text-center" ng-show="selections.length > 0">Menos importante</span>-->
                        </div>
                    </div>
                </div>
            </div>
            <div class="row margin-top-24" ng-if="stages[selectedStage].id == currentStageId">
                <div class="col-xs-12 text-center">
                    <button class="btn btn-default" ng-click="sendActorSel(true)">
                        <i class="fa fa-check" ng-show="verified"></i>
                        <i class="fa fa-send" ng-show="!verified"></i>
                        <span ng-show="!verified">Por Enviar</span>
                        <span ng-show="verified">Enviado</span>
                    </button>
                </div>
            </div>
        </div>
        <br>
        <div ng-if="stages[selectedStage].id == currentStageId && prevRes != null">
            <div ng-repeat="res in prevRes">
                <hr>
                <h3>{{ 'stage' | lang }} {{ stagesMap[prevStages[$index]].number }}</h3>
                <div ng-repeat="(uid, arr) in res">
                    <h4 class="anon-badge">{{ teamMap[uid] }}</h4>
                    <div class="row">
                        <div class="col-sm-10 col-sm-offset-1 col-xs-12">
                            <div>
                                <div class="list-group">
                                    <div class="list-group-item drag-item" ng-repeat="actor in arr">
                                        <div class="row">
                                            <div class="col-xs-2 col-sm-1 h30 text-center">
                                                {{$index + 1}}.
                                            </div>
                                            <div class="col-xs-6 col-sm-7 mh30">
                                                <strong>{{ actor.name }}</strong>
                                            </div>
                                            <div class="col-xs-4 text-right">
                                                <button class="btn btn-default btn-sm" ng-click="actor.expanded = !actor.expanded" ng-if="actor.description">
                                                    <i class="fa fa-eye"></i>
                                                </button>
                                            </div>
                                        </div>
                                        <div class="row" ng-show="actor.expanded">
                                            <div class="col-xs-12">
                                                {{ actor.description }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div ng-if="stages[selectedStage].id != currentStageId && stages[selectedStage].type == 'team' && prevResPrev != null">
            <div ng-repeat="res in prevResPrev">
                <hr>
                <h3>{{ 'stage' | lang }} {{ stagesMap[prevStagesPrev[$index]].number }}</h3>
                <div ng-repeat="(uid, arr) in res">
                    <h4 class="anon-badge">{{ teamMapPrev[uid] }}</h4>
                    <div class="row">
                        <div class="col-sm-10 col-sm-offset-1 col-xs-12">
                            <div>
                                <div class="list-group">
                                    <div class="list-group-item drag-item" ng-repeat="actor in arr">
                                        <div class="row">
                                            <div class="col-xs-2 col-sm-1 h30 text-center">
                                                {{$index + 1}}.
                                            </div>
                                            <div class="col-xs-6 col-sm-7 mh30">
                                                <strong>{{ actor.name }}</strong>
                                            </div>
                                            <div class="col-xs-4 text-right">
                                                <button class="btn btn-default btn-sm" ng-click="actor.expanded = !actor.expanded" ng-if="actor.description">
                                                    <i class="fa fa-eye"></i>
                                                </button>
                                            </div>
                                        </div>
                                        <div class="row" ng-show="actor.expanded">
                                            <div class="col-xs-12">
                                                {{ actor.description }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <br>
        <div ng-show="stages[selectedStage].chat">
        <div class="chat-inv"></div>
        <div class="chat-container">
            <div class="chat bordered">
                <div class="text-center">Chat</div>
                <div class="chat-area" scroll-glue ng-show="chatExp" ng-if="stages[selectedStage].id == currentStageId">
                    <strong ng-show="chatMsgs[currentStageId].length == 0">Chat</strong>
                    <div class="chat-msg" ng-repeat="msg in chatMsgs[currentStageId]" ng-class="{'bg-gray': chatmsgreply == msg.id}">
                        <div class="font-mini parent-msg" ng-show="msg.parent != null">
                            <div class="anon-badge margin-left-n8">{{ teamMap[msg.parent.uid] }}</div>
                            <span class="content" ng-bind-html="msg.parent.content | limitTo:100 | linkfy"></span>
                        </div>
                        <i class="fa fa-reply" ng-show="msg.parent != null"></i>
                        <div class="anon-badge" ng-class="{'margin-left-n8': msg.parent == null, 'simple-margin-left': msg.parent != null}">{{ teamMap[msg.uid] }}</div>
                        <span class="pull-right font-mini text-right">
                            <span class="text-muted">{{msg.stime | date:'HH:mm dd-MM-yy'}}</span>
                            <br>
                            <a ng-click="setReply(msg)">{{'reply' | lang}}</a>
                        </span>
                        <span class="content" ng-bind-html="msg.content | linkfy"></span>
                    </div>
                </div>
                <div class="chat-area" scroll-glue ng-show="chatExp" ng-if="stages[selectedStage].id != currentStageId">
                    <strong ng-show="chatMsgsPrev[currentStageId].length == 0">Chat</strong>
                    <div class="chat-msg" ng-repeat="msg in chatMsgsPrev[stages[selectedStage].id]">
                        <div class="font-mini parent-msg" ng-show="msg.parent != null">
                            <div class="anon-badge margin-left-n8">{{ teamMapPrev[msg.parent.uid] }}</div>
                            <span class="content" ng-bind-html="msg.parent.content | limitTo:100 | linkfy"></span>
                        </div>
                        <i class="fa fa-reply" ng-show="msg.parent != null"></i>
                        <div class="anon-badge" ng-class="{'margin-left-n8': msg.parent == null, 'simple-margin-left': msg.parent != null}">{{ teamMapPrev[msg.uid] }}</div>
                        <span class="pull-right font-mini text-right">
                            <span class="text-muted">{{msg.stime | date:'HH:mm dd-MM-yy'}}</span>
                        </span>
                        <span class="content" ng-bind-html="msg.content | linkfy"></span>
                    </div>
                </div>
                <div class="input-group">
                    <span class="input-group-btn">
                        <button class="btn btn-default" ng-click="chatExp = !chatExp">
                            <i class="fa fa-arrow-up" ng-show="!chatExp"></i>
                            <i class="fa fa-arrow-down" ng-show="chatExp"></i>
                        </button>
                        <button class="btn btn-primary" ng-show="chatmsgreply != null" ng-disabled="finished || stages[selectedStage].id != currentStageId" ng-click="setReply(null)">
                            <i class="fa fa-reply"></i>
                        </button>
                    </span>
                    <input type="text" class="form-control" ng-model="chatmsg" ng-disabled="finished || stages[selectedStage].id != currentStageId" id="chat-input"
                           ng-keydown="$event.keyCode === 13 && sendChatMsg()" placeholder="Enviar mensaje...">
                        <!--  <ng-quill-editor ng-model="newText.content" class="large" ng-class="{'quill-disabled': selectedSes.status!=1}"></ng-quill-editor>-->
                    <span class="input-group-btn">
                        <button class="btn btn-default" ng-disabled="finished || stages[selectedStage].id != currentStageId" ng-click="sendChatMsg()">
                            <i class="fa fa-send"></i>
                        </button>
                    </span>
                </div>
            </div>
        </div>
        </div>
    </div>

    <br>
    </div>
    <br>
    </div>

    <div class="splash" ng-if="iteration <= 0">
        <div class="splash-box">
            <h3><i class="fa fa-clock-o"></i> Sesión en espera</h3>
            <h5>Espere a que el profesor indique el inicio de la actividad.</h5>
            <h5>Esta página se recargará automáticamente cuando comience.</h5>
        </div>
    </div>

</div>

</body>
</html>
