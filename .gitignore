.idea/
.DS_store
env-files.tar.gz

# Ignorar secretos
.env
.env.*

# Permitir templates
!.env.example
!.env.*.example

# Secret files, but keep directory
secrets/
!secrets/.keep

# Log files and root node modules (for linters)
*.log
*.lock
node_modules/
*.backup

.codegpt